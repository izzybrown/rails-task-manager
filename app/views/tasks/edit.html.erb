<%= form_for @task do |t| %>
  <%= t.label :title, "Enter a title" %>
  <%= t.text_field :title %>

  <%= t.label :details, "Enter the details" %>
  <%= t.text_field :details %>

  <%= t.submit %>

<% end %>
